---
description: Repo constitution — planning corpus; VERIFY_STACK Must spine; no product code yet
alwaysApply: true
---

# Verified Architecture — constitution

You are in a **planning + RAG corpus** for a verified polyglot architecture
product. Progressive disclosure is part of the product.

## Cold start

No prior context → `AGENT_BOOTSTRAP.md` → `STATUS.md` → `AGENT_WALKTHROUGH.md`
→ `STRUCTURE.md`. Skill `cold-start`.

## Identity

- **Working draft** under ISO/IEC/IEEE 29148-shaped RE, SEI ATAM QAS,
  Nygard ADRs, and C4. Draft *content* is fine; skipping the *form* is not.
- **Must spine:** graph + locks **∧** EA-Graph claim memory **∧** STEAD tool
  constraints **∧** receipts — `08-verification/VERIFY_STACK.md`.
  Do **not** shrink to graph+locks alone.
- **Polyglot as options** under `07-system-design/options/`.
- **`research/` is evidence** — retrieve one pack; never always-load.

## Gates

- No product code until DoR + no-code-gate green.
- Must NFRs → six-part QAS before Design.
- Constraints ≠ requirements; `blocks_code` OQs block codegen.
- Unknown / **unprovable** > wrong; evidence ≠ freshness.
- MCP/CLI entity ids typed from stores (STEAD ST-1…5) — no hallucinated handles.
- RAG/LLM text ∉ witnesses or claim anchors.
- C4 cites ADR IDs; science = locked transfers only.

## Context budget

Always-on ≤2. Path scope = MDC globs. Depth = Skills.
Prefer `00/`–`12/` over legacy `docs/` + `nests/`.
