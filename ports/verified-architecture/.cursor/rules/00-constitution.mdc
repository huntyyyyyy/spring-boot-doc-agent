---
description: Repo constitution — polyglot planning + RAG corpus; no product code yet
alwaysApply: true
---

# Verified Architecture — constitution

You are in a **planning + RAG corpus** for a verified polyglot architecture
product. Progressive disclosure is part of the product.

## Identity

- **Working draft** under ISO/IEC/IEEE 29148-shaped RE, SEI ATAM QAS,
  Nygard ADRs, and C4. Draft *content* is fine; skipping the *form* is not.
- **Polyglot-first:** Rust, WASM, SQLite, Go, Ruby, Clojure, TypeScript,
  Python as peer, C/Zig when earned — not a Python-majority product.
- **`research/` is evidence for RAG and humans** — retrieve it; do not
  always-load it; do not discard it as fluff.

## Gates

- No product code until `CONTRIBUTING.md` checklist is green (ADR-0008).
- Must NFRs need six-part QAS before influencing Design.
- Constraints ≠ requirements (`docs/constraints/`).
- C4 structural claims cite ADR IDs.
- Unknown > wrong; claim tiers Evidenced / Confirmed / Unknown.

## Context budget

Always-on rules ≤2. Path scope = MDC `globs` + nest MDCs. Depth = Skills.
Open `docs/DOMAIN_MAP.md` before broad exploration.
