---
description: Cold-start priming — pull when session lacks prior context or agent seems lost
alwaysApply: false
---

# Cold start

If you lack prior chat context for this repo:

1. Read `AGENT_BOOTSTRAP.md` then `STATUS.md`.
2. Follow Skill `cold-start`.
3. Do not generate product code; fill the next FAIL DoR / OQ instead.
