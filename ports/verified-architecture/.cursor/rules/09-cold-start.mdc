---
description: Cold-start priming — pull when session lacks prior context or agent seems lost
alwaysApply: false
---

# Cold start

If you lack prior chat context for this repository:

1. Read `AGENT_BOOTSTRAP.md` then `STATUS.md` then `GLOSSARY.md`.
2. Follow Skill `cold-start`.
3. Use **whole words** in every edit (no bare acronyms in prose).
4. Do not generate product code; fill the next FAIL Definition of Ready /
   open-question row instead.
