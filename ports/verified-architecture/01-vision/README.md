# 01-vision

| Artifact | State |
| --- | --- |
| `problem-frame/BOUNDARY.md` | Present — System of Record for product boundary |
| `non-goals/` | **Missing** — `.gitkeep` only |
| `success-measures/` | **Missing** — `.gitkeep` only |

Code before boundary Accept → reject (`DEFINITION_OF_READY.md`).
