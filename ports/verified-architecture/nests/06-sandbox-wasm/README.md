# Nest: WebAssembly sandbox

**Owns (when built):** Optional LockCheck **guest** (Could / Wave-3) —
fuel/epoch; trust-boundary (not unearned proved). **Refuse** Spec corpus Model
Context Protocol host.

**Fail closed:** WASM as Spec corpus MCP host, or treating guest as Wave-1
Must without gate + Component C4 → violates Architecture Decision Record
ADR-0004 (amended).

**Now:** README + `nest.mdc` only — no guest crate until Definition of Ready
PASS. Not Wave-1 Must.

## Open first

1. `docs/adr/adr-0004-native-then-wasm-lockcheck.md`  
2. `docs/c4/02-containers.md`

## Shared System of Record

- `03-requirements/` · `04-constraints/`  
- `docs/c4/` · `docs/adr/`
