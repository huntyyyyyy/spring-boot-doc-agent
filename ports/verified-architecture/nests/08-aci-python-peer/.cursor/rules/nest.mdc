---
description: Nest context for Python peer — path-scoped; do not load other nests
globs:
  - nests/08-aci-python-peer/**
  - ports/verified-architecture/nests/08-aci-python-peer/**
alwaysApply: false
---

# Nest: Python peer

When files under `nests/08-aci-python-peer/` are in context:

1. Read this nest `README.md` only.
2. Open: Architecture Decision Record ADR-0001 peer lane.
3. Owns: Peer ACI only — not automatic Spec MCP Why.
4. Obey constitution FREEZE — no product scaffolds; no unearned Adopt.
5. Long evidence → Skill `rag-retrieve` + `research/INDEX.md` (**one** pack).
6. Do **not** open every other nest unless integrating across bounded contexts.
7. Whole words — root `GLOSSARY.md`.
