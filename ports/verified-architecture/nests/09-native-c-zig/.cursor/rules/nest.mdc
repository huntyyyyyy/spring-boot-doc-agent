---
description: Nest context for C / Zig niche — path-scoped; do not load other nests
globs:
  - nests/09-native-c-zig/**
  - ports/verified-architecture/nests/09-native-c-zig/**
alwaysApply: false
---

# Nest: C / Zig niche

When files under `nests/09-native-c-zig/` are in context:

1. Read this nest `README.md` only.
2. Open: Architecture Decision Record ADR-0001 niches.
3. Owns: Systems/WebAssembly niches — Refuse Spec MCP day-one.
4. Obey constitution FREEZE — no product scaffolds; no unearned Adopt.
5. Long evidence → Skill `rag-retrieve` + `research/INDEX.md` (**one** pack).
6. Do **not** open every other nest unless integrating across bounded contexts.
7. Whole words — root `GLOSSARY.md`.
