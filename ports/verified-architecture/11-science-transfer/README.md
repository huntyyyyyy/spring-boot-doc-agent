# 11-science-transfer

| Subdir | What belongs | Missing / bound |
| --- | --- | --- |
| `locked-transfers/` | Allowed theory→product metaphors + refuse substrates | Filled — edit there |
| `refuse-substrates/` | Pointer into locked-transfers physical table | No separate ledger yet |
| `papers/` | Pointer only | Digests live under `research/papers-2026-may-aug/` |

New substrate Adopt as tip kernel / CI floor → reject unless an Architecture
Decision Record Accepts it and FREEZE allows (it does not today).
