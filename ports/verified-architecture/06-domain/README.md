# 06-domain

| Subdir | Artifact | State |
| --- | --- | --- |
| `ubiquitous-language/` | `UNKNOWN-TAXONOMY.md` | Draft codes only |
| `bounded-contexts/` | context map / BC list | **Missing** — `.gitkeep` only |
| `information-model/` | entity / relation sketch | **Missing** — `.gitkeep` only |

Do not invent bounded-context ownership here to unblock code. Stack locks stay
in Architecture Decision Records / `STATUS.md` (Rust Spec host; TypeScript IDE;
WebAssembly LockCheck Could).
