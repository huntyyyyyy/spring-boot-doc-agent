# Maturity Assessment

A standing, in-place-edited scorecard for anyone evaluating **doc-engine** (this monorepo) for use beyond a single operator's own machine — a different axis from `CONSTRAINTS.md` (the catalog of individual facts this scorecard's ratings are grounded in) and `STATUS.md` (done/pending tracking for the steering-prompt work driving this repo's own evolution). The Claude Code plugin under `adapters/claude/` is an optional generative adapter, not the whole product — see [`docs/product-architecture.md`](docs/product-architecture.md). Read this file for "how mature is this, overall, and what has to be true before wider adoption"; read the other two for the itemized facts behind that judgment.

Written 2026-07-23, after adding `skills/semantic-pipeline-eval/` and `skills/capacity-preflight/` (see `docs/process/session-log.md`'s entry for this date). Every rating below cites a real file, test count, or documented gap — not an unverified impression — the same evidence discipline `doc-taxonomy.md`'s `[Evidenced]`/`[Unknown]` tagging already enforces on generated documentation output.

---

## Scorecard

| Dimension | Rating | Grounded in |
|---|---|---|
| Testing depth | **Moderate-to-strong, mechanical; semantic layer newly scaffolded, not yet exercised against a real run** | Every suite under `tests/` passes locally and all but the opt-in ones are wired into `.github/workflows/ci.yml`. For the current inventory and count run `python -c "from pathlib import Path; print(len(list(Path('tests').rglob('test_*.py'))))"` and `pytest tests/ -q` rather than trusting a number here — the figure previously hardcoded in this cell ("Eleven suites … 194 tests") had gone stale in both directions, naming a `test_verify_llms_docs.py` that was since deleted while undercounting the suites that replaced it. `tests/doc_engine/test_pipeline_stages.py` is explicitly mechanical (tag grammar, citation resolvability, JSON shape) — it does not judge truthfulness. `skills/semantic-pipeline-eval/` now provides that judgment layer, but it's an LLM-driven skill by nature, not a headless CI check — it has never been run against a real completed pipeline output as of this writing. |
| Scalability / load-tested-ness | **Stage 0 measured on one real service; Stages 1-4 still unvalidated at scale** | `skills/capacity-preflight/` turns `CONSTRAINTS.md`'s prose scale-assumptions (chars/N token heuristic, uncapped fan-out, per-group `cross_group_edges.json` slice) into concrete numbers via `src/doc_engine/tools/capacity_preflight.py`, which imports (does not re-derive) `doc_engine.tools` partition/scan/edges modules. **[Updated 2026-07-24]** First run against a real Spring service (615 Java files, Gradle, Boot 2.7): 18 groups, 52 dispatches, largest Stage-1 edge slice ~87,600 est. tokens against a 120,000 per-group budget — all three thresholds crossed. Two findings from that run: the group/fan-out thresholds (15/40) are *low* relative to an ordinary mid-size service and want recalibration upward or a documented rationale; and the third threshold was measuring the removed broadcast, overstating it ~21x, now replaced by a largest-single-slice threshold (30,000). Only Stage 0 has been exercised — the four LLM stages have still never run at this scale, so per-dispatch context behavior and return-payload cost remain unmeasured. |
| Schema & contract rigor between stages | **Partially resolved — 2026-07-28** | Pydantic models (`src/doc_engine/pipeline/artifacts.py`), JSON Schema exports (`scripts/schemas/`), and `src/doc_engine/tools/validate_artifacts.py` now enforce the four inter-stage artifacts; `src/doc_engine/tools/pipeline_validators.py` ships the summarizer/gap-analyzer shape gates; `skills/document-spring-repo/SKILL.md` documents contracts and boundary validation. Residual gap: CI does not gate a full live pipeline run's artifacts (no target-repo run in CI) — only the committed `spring_signals` fixture plus unit tests (`tests/doc_engine/test_artifact_schemas.py`, `tests/doc_engine/test_pipeline_stages.py`). |
| Observability / telemetry | **Moderate — pre-run estimate and post-run record both exist now, neither calibrated against a real run yet** | `src/doc_engine/tools/run_manifest.py` (`CONSTRAINTS.md` "Integration gaps" item 3, closing `docs/process/steering-prompts/04-analytics-logging-research-prompt.md` item 2) writes a `run_manifest.json` once per pipeline invocation: per-stage timing/pass-fail/canceled state, evidence-tag counts per generated file, interview answered/skipped breakdown, and target-repo commit hash + dirty flag + `file_signatures`. It also closes the specific calibration-loop gap this row previously named: `finalize --preflight-file capacity_preflight_report.json` records `capacity-preflight`'s *predicted* per-stage fan-out next to each stage's own recorded *actual* fan-out (via an explicit key-mapping table, since `capacity_preflight.py`'s stage-key vocabulary — `stage1_file_summarizer` etc. — doesn't match `run_manifest.py`'s own stage names). **[Updated 2026-07-24]** `capacity_preflight.py` has now run against a real 615-file service; `run_manifest.py` has not, and the predicted-vs-actual calibration loop still has not closed once, because it needs the four LLM stages to record actual fan-out and only Stage 0 has been run. So the loop remains mechanically present and empirically unexercised — but the pre-run half of it is no longer fixture-only. |
| Security & governance | **Weak** | No RBAC or auth layer (`CONSTRAINTS.md` "Enterprise-readiness gaps" item 1 — confirmed by absence). Run-manifest telemetry exists with stated residuals (`CONSTRAINTS.md` Integration item 3) but is not a full audit trail. Secret/credential redaction exists and is tested (`src/doc_engine/scanning/support/_secret_heuristics.py`, `tests/doc_engine/test_secret_heuristics.py`) but is explicitly heuristic, not exhaustive. License field in `.claude-plugin/plugin.json` is `MIT`. |
| Dependency reproducibility | **Resolved, 2026-07-25 — dependencies now pinned in `requirements.txt`** | `requirements.txt` at the monorepo root now pins `sqllineage~=1.5.8`, `pathspec~=1.1.1`, `semgrep~=1.171.0` (compatible-release specifiers, not exact pins). CodeQL CLI is intentionally *not* in `requirements.txt` because it is a standalone binary release, not a Python package; operators must install it separately from https://github.com/github/codeql-cli-binaries/releases. `.github/workflows/ci.yml` installs the Python dependencies (`pip install -r requirements.txt`) and assumes a CodeQL CLI is present on the runner. **2026-07-24 finding, now closed**: a fresh environment without `sqllineage` installed had failed 7/32 tests in `tests/doc_engine/test_spring_signal_scan.py` — a live reproduction of this exact gap, not a defect introduced by the PR under review at the time. Installing it brought those tests to a clean pass, and the pinned-install fresh-environment repro (re-run 2026-07-25) confirms the same clean pass from `requirements.txt` alone. This reclassifies the adoption gate checklist's dependency-pinning item from blocking to done. |
| Documentation & handoff quality | **Strong** | `CONSTRAINTS.md`, `STATUS.md`, `CONTRIBUTING.md`, `docs/process/session-log.md`, and `skills/document-spring-repo/references/doc-taxonomy.md` collectively give a new contributor (or a cold session) a single, cross-linked place to learn this repo's real state rather than reconstructing it from commit history — the same design principle this file follows. |

## Drift from modern practice

- **Prose-only inter-stage contracts vs. schema-validated contracts.** This repo now validates the four inter-stage JSON artifacts via Pydantic + `validate_artifacts.py` (see scorecard row above). Residual drift risk: live pipeline outputs are not schema-gated in CI.
- **Mechanical-only testing vs. semantic-eval harnesses for LLM pipelines.** `tests/doc_engine/test_pipeline_stages.py`'s own commit (per `docs/process/session-log.md`) cites real precedent for this gap: arXiv:2604.25359 shows schema compliance and value accuracy diverge sharply in structured LLM output, and `promptfoo` (23.5k+ GitHub stars, confirmed via `gh api`) is a current, maintained example of the eval-harness-over-LLM-judge pattern this project already leans toward. `skills/semantic-pipeline-eval/` is a first step toward closing this specific drift — genuinely new capability, not previously present in any form — but it is a manually-invoked skill, not a CI-integrated harness; a promptfoo-style automated eval gate on every prompt change remains open.
- **CI without branch-protection enforcement vs. required-status-checks as baseline practice.** `.github/workflows/ci.yml` runs a real, passing test suite on every PR, but `CONSTRAINTS.md` Enterprise item 3 confirms that nothing requires it to pass before merge. Baseline modern practice treats "CI exists" and "CI is enforced" as two separate, both-required steps; this repo has only the first.
- **[Resolved] Unpinned dependencies vs. lockfiles as baseline practice.** `requirements.txt` now pins all three third-party dependencies with `~=` compatible-release specifiers — meets the modern minimum-bar expectation for anything beyond a single-operator script.

## Named limitations and paths forward

**What the two new skills concretely close:**
- A previously-nonexistent semantic/hallucination-detection layer for the four LLM pipeline stages (`skills/semantic-pipeline-eval/`), directly extending the scope `docs/process/steering-prompts/01-testability-research-prompt.md` originally named and only partially closed (mechanical checks only).
- A previously-nonexistent, per-repo, measurable answer to "will this run be too big" (`skills/capacity-preflight/`), replacing prose assumptions in `CONSTRAINTS.md`/`SKILL.md` with actual numbers for a specific target repo.

**What remains fully open — not touched by either new skill:**
- ~~Schema/contract validation between pipeline stages (`docs/process/steering-prompts/02`).~~ **Closed 2026-07-28** — see scorecard row and `docs/process/steering-prompts/02-pluggability-research-prompt.md` verify predicates.
- RBAC, multi-repo/batch support, audit trail, and branch-protection enforcement — all still exactly as open as `CONSTRAINTS.md`'s "Enterprise-readiness gaps" section already states, in the same close-out order that file recommends. (Dependency pinning, formerly on this list, is now resolved — see the scorecard row above.)

**Closed since the above was first written:** post-run telemetry / `run_manifest.json` (`docs/process/steering-prompts/04` item 2) — `src/doc_engine/tools/run_manifest.py`, including the predicted-vs-actual fan-out tie-in this section originally flagged as a natural follow-on. See the "Observability / telemetry" scorecard row above for what's still unexercised (no real-repo run yet) and `CONSTRAINTS.md` "Integration gaps" item 3 for the residual `spring_drift_check.py` integration gap.
- CI-automated semantic evaluation — `semantic-pipeline-eval` is manually invoked today; it cannot run headless in `.github/workflows/ci.yml` the way the mechanical suites do, since it requires a live LLM-driven session.

## Adoption gate checklist

Minimum concrete items before this pipeline is adopted beyond a single operator's own machine, ordered to match `CONSTRAINTS.md`'s existing close-out sequence:

- [ ] License resolved and consistently documented — `.claude-plugin/plugin.json` already says `MIT`; `CONSTRAINTS.md`/`STATUS.md` corrected to match as of this change, but re-confirm before relying on either doc.
- [ ] Branch protection enabled on `main` requiring the existing CI workflow to pass and at least one review (`CONSTRAINTS.md` Enterprise item 3; admin `gh api` action, owner-deferred).
- [x] Dependencies pinned (`requirements.txt` for `ast-grep-cli`, `sqllineage`, `pathspec`) — **done, 2026-07-25**: closes the gap `CONSTRAINTS.md` item 4's 2026-07-24 finding had reclassified from aspirational to blocking.
- [ ] `skills/semantic-pipeline-eval/` run at least once against a real completed pipeline run (not just synthetic fixtures), and its findings reviewed by a human.
- [~] `skills/capacity-preflight/` run against the largest repo the organization actually intends to point this at, and its warning thresholds reviewed/recalibrated against that real result rather than left at their stated-guess defaults. **Partially done 2026-07-24** — run against one real 615-file service, which invalidated rather than calibrated the third threshold (it measured a broadcast removed in `abd3ade`; replaced, not retuned). The group-count and fan-out thresholds were crossed but are *not* yet recalibrated: one data point cannot distinguish "threshold too low" from "repo genuinely large." Still open until run against the actual largest intended target.
- [ ] Run-level audit trail / `run_manifest.json` built (`docs/process/steering-prompts/04`).
- [ ] RBAC / multi-repo support addressed, if the intended usage is multi-team rather than single-operator (lowest urgency of this list per `CONSTRAINTS.md`'s own ordering).

## Cross-links

- `CONSTRAINTS.md` — the plugin's standing constraints; this file's ratings are grounded in its entries.
- `STATUS.md` — current-state done/pending tracking for the steering-prompt work.
- `docs/process/session-log.md` — append-only history; see the entry for this file's own addition.
- `skills/semantic-pipeline-eval/SKILL.md`
- `skills/capacity-preflight/SKILL.md`
