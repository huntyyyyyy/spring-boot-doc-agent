---
pr: 28
title: "Sync status docs, fix ast-grep test-killing bug, resolve bounded JPQL lineage"
state: MERGED
branch: sync-status-ast-grep-jpql-lineage -> main
head_commit: 9d15ed3
merge_commit: 03c16ddff31a6a12b5b9ca90f6fe2120fe517a6f
---

# PR #28 — Sync status docs, fix ast-grep test-killing bug, resolve bounded JPQL lineage

## Summary

Four related pieces of work reviewing `CONSTRAINTS.md` against the repo's actual current state, from one session:

1. `STATUS.md` and steering-prompts `03`/`04`/`08` had gone stale relative to `claude/session-log.md` — dependency pinning, run-manifest telemetry, and the `references/doc-taxonomy.md` path were already resolved but still described as pending/wrong.
2. `find_ast_grep()` called `sys.exit(1)` directly, silently killing the whole test process (not just one test class) whenever `ast-grep` was missing, since `SystemExit` isn't caught by `unittest`'s `setUpClass` handler. Now raises `AstGrepNotFoundError`, an ordinary exception; the three CLI entry points that call it catch it explicitly to preserve the original stderr-message/exit-1 behavior.
3. JPQL `@Query` values never got SQL-lineage extraction, even though `entity_table_map` already had everything needed for the common single-entity case. `resolve_jpql_to_lineage()` now closes that case; joins, association traversal, JPQL-only functions, entity-name overrides, polymorphic `FROM`, and embedded keys remain explicitly out of scope.
4. Item 3 introduced a real drift-check blind spot: a JPQL citation's lineage is derived from two files (the query's own, and the entity's), but `spring_drift_check.py`'s tier-1/tier-2 model only ever checked one. Fixed by naming the actual invariant — a citation is fresh iff every file in its provenance is unchanged — and widening it for JPQL rather than bolting on a special case. See `CONSTRAINTS.md`'s JPQL entry and `spring_drift_check.py`'s own "DERIVED CITATIONS HAVE MORE THAN ONE INPUT" docstring section for the full design.

## Deterministic verification

Pinned to `824b3b7` (branch head; still open, per `claude/llms/README.md`'s "still-open PR" convention):

1. **Claim: `find_ast_grep()` no longer calls `sys.exit(1)`, and raises `AstGrepNotFoundError` instead.**
   `git show 824b3b7:scripts/spring_signal_scan.py | grep -n "class AstGrepNotFoundError\|raise AstGrepNotFoundError\|sys.exit(1)" `
   Expect: `class AstGrepNotFoundError(RuntimeError):` and `raise AstGrepNotFoundError(` present; no `sys.exit(1)` inside `find_ast_grep()` itself (the CLI `except` blocks in `main()` do still call `sys.exit(1)`, so this grep will show a couple of unrelated `sys.exit(1)` hits elsewhere in the file — confirm none of them sit inside `find_ast_grep()`'s own body by checking the line numbers against `def find_ast_grep():`).

2. **Claim: all three CLI entry points that call into `find_ast_grep()`/`scan()`/`check_drift()` now catch `AstGrepNotFoundError`.**
   `git show 824b3b7 -- scripts/spring_signal_scan.py scripts/spring_drift_check.py scripts/capacity_preflight.py | grep -n "except.*AstGrepNotFoundError"`
   Expect: three matches, one per file.

3. **Claim: `resolve_jpql_to_lineage()` is new, and is wired into `scan()` in a pass separate from the native-query branch (deferred until after `entity_table_map` is fully built).**
   `git show 824b3b7:scripts/spring_signal_scan.py | grep -n "def resolve_jpql_to_lineage\|JPQL lineage resolution happens in its own pass"`
   Expect: both present, the docstring/comment explaining why it's a separate pass (ast-grep match order isn't guaranteed to process an entity's match before a query match that references it).

4. **Claim: `test_spring_signal_scan.py` grew from 32 to 40 tests, including a dedicated `JpqlLineageResolutionTest` class.**
   `git show 824b3b7:scripts/test_spring_signal_scan.py | grep -c "    def test_"`
   Expect: `40`.
   `git show 824b3b7:scripts/test_spring_signal_scan.py | grep -n "class JpqlLineageResolutionTest"`
   Expect: one match.

5. **Claim: the real fixture's JPQL query resolves to `billing_invoice` lineage.**
   `git show 824b3b7:scripts/test_spring_signal_scan.py | grep -A6 "def test_jpql_query_resolves_lineage_via_entity_table_map"`
   Expect: assertions on `jpql["lineage"]["available"]` being true and `jpql["lineage"]["source_tables"] == ["billing_invoice"]`.

6. **Claim: `CONSTRAINTS.md`'s JPQL precision-tradeoff item and `STATUS.md`'s pending list were both updated, not left stale.**
   `git show 824b3b7:CONSTRAINTS.md | grep -n "Resolved for the bounded common case"`
   `git show 824b3b7:STATUS.md | grep -n "Last updated: 2026-07-25"`
   Expect: one match each.

### Item 4 — JPQL-lineage drift-check provenance fix

Pinned to `2cfb4e0` (branch head):

7. **Claim: `resolve_jpql_to_lineage()` stamps `resolved_via_entity` on a resolved lineage result.**
   `git show 2cfb4e0:scripts/spring_signal_scan.py | grep -n "resolved_via_entity"`
   Expect: at least two hits — the assignment inside `resolve_jpql_to_lineage()` and its mention in the schema_version 6 docstring note.

8. **Claim: `_recheck_entities()` and `tier2_recheck_file()` now return the fresh entity data they compute, not just their results list.**
   `git show 2cfb4e0:scripts/spring_drift_check.py | grep -n "return results, fresh_entities\|return results, fresh_entity_tables"`
   Expect: one match each.

9. **Claim: a new post-loop pass (`_reverify_jpql_lineage_provenance`) exists and is called once from `check_drift()`, after the per-file loop, not inside it.**
   `git show 2cfb4e0:scripts/spring_drift_check.py | grep -n "def _reverify_jpql_lineage_provenance\|_reverify_jpql_lineage_provenance(results, signals, fresh_entity_tables, changed_set)"`
   Expect: the `def` plus exactly one call site (inside `check_drift()`).

10. **Claim: the fix reuses `STATUS_CONFIRMED`/`STATUS_DRIFTED` — no new status constant was added.**
    `git show 2cfb4e0:scripts/spring_drift_check.py | grep -n "^STATUS_"`
    Expect: the same set of `STATUS_*` constants as before this PR (no `STATUS_DEPENDENT_ENTITY_CHANGED` or similar new name present).

11. **Claim: `test_spring_drift_check.py` grew from 22 to 36 tests, including a dedicated `JpqlLineageProvenanceTest` class with isolated unit tests (no `ast-grep`/tempdir).**
    `git show 2cfb4e0:scripts/test_spring_drift_check.py | grep -c "    def test_"`
    Expect: `36`.
    `git show 2cfb4e0:scripts/test_spring_drift_check.py | grep -n "class JpqlLineageProvenanceTest"`
    Expect: one match.

12. **Claim: the false-positive-avoidance case is explicitly tested — an entity file changing without its table mapping actually moving must CONFIRM the JPQL lineage, not flag it as drifted.**
    `git show 2cfb4e0:scripts/test_spring_drift_check.py | grep -A3 "def test_entity_file_changed_but_table_mapping_unchanged_confirms_jpql_lineage"`
    Expect: an edit to an unrelated comment in `Invoice.java`, then an assertion the JPQL result's status is `STATUS_CONFIRMED`.
