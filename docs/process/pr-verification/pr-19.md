---
pr: 19
title: "CONSTRAINTS.md: add solo-context note; flag coverage-exemption heuristic as provisional"
state: MERGED
branch: constraints-solo-context-and-flag -> main
merge_commit: 0d7f7275ce5b0712e5ac8e925a29abf2926ee08a
---

# PR #19 — CONSTRAINTS.md: add solo-context note; flag coverage-exemption heuristic as provisional

## Summary

Doc-only PR, two edits to `CONSTRAINTS.md` per direct repo-owner feedback. **(1)** "Enterprise-readiness gaps" item 6 ("no required reviews/branch protection") read as a flat deficiency without acknowledging there are no other contributors today — added a note that `CONTRIBUTING.md`'s write-then-verify rule is the effective substitute for review while solo, not an absence of scrutiny; doesn't remove the gap, still names the concrete `gh api` fix for when the project gains contributors. **(2)** Flags the PR #18 exemption heuristic ("exempt the single most-recently-merged PR, unconditionally") as provisional per the repo owner's own read — it terminates the regress but is a blunt instrument. Names two candidate sharper edges (time-bounded expiry; distinguishing "hasn't had a chance yet" from "already missed its window") without committing to either, deliberately deferred rather than iterated on speculatively.

Outside the `scripts/`/`agents/`/`skills/`/`references/` trigger `claude/llms/README.md`'s same-PR-doc convention names, so this PR didn't carry its own `pr-19.md` at merge time — it fell to the grace window instead, same as any other small PR, and is being closed now after the window shifted forward past it.

## Deterministic verification

Pinned to `0d7f727`:

1. **Claim: exactly one file changed — CONSTRAINTS.md, 4 lines added, doc-only.**
   `git show --stat --format="" 0d7f727`
   Expect: `CONSTRAINTS.md | 4 ++++`, `1 file changed, 4 insertions(+)`.

2. **Claim: adds the solo-context note to item 6.**
   `git show 0d7f727:CONSTRAINTS.md | grep -n "Current-context note"`
   Expect: one match, referencing `CONTRIBUTING.md`'s write-then-verify rule as the substitute for review while solo.

3. **Claim: flags the exemption heuristic as provisional, without resolving it.**
   `git show 0d7f727:CONSTRAINTS.md | grep -n "Flagged, not yet resolved"`
   Expect: one match, naming both candidate refinements (time-bounded expiry; hasn't-had-a-chance vs. missed-its-window) and explicitly deferring rather than picking one.
