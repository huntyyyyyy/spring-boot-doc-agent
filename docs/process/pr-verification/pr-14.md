---
pr: 14
title: "Land two commits stranded after PR #13 merged early"
state: MERGED
branch: land-stranded-commits -> main
merge_commit: b8d07f903062b91aad4b7bea98c5d8dfd221fc4d
---

# PR #14 — Land two commits stranded after PR #13 merged early

## Summary

PR #13 merged into `main` at `13553ba` while two more commits (`f3af862`, `aa3ae9e`) were still being pushed to its branch in the same session, so they never made it in. This PR cherry-picks both: `f3af862` adds `claude/steering-prompts/08-dependency-pinning-task-prompt.md` (scoping the `ast-grep-cli`/`sqllineage`/`pathspec` pinning work) and fixes two stale "license still UNLICENSED" lines in `STATUS.md`; `aa3ae9e` logs a `claude/tool-quirks.md` entry documenting that `gh api repos/.../pulls/<N>` can report a stale `.head.sha` for 30+ seconds after a fast push even though `git ls-remote`/the `compare` endpoint reflect the real branch state immediately — the exact staleness that caused these two commits to go unnoticed as unmerged. Also adds `claude/steering-prompts/09-tool-quirks-indexing-research-prompt.md`. Verified via `git merge-base --is-ancestor` that both original commits weren't yet ancestors of `origin/main` before this PR, and every other remote branch was checked against every merged PR for the same pattern (none found — specific to PR #13 continuing to receive pushes after it had already merged).

## Deterministic verification

Pinned to `b8d07f9`:

1. **Claim: exactly 4 files touched — STATUS.md, two new steering-prompt files, and `claude/tool-quirks.md` (historical path at that commit).**
   `git show --stat --format="" b8d07f9`
   Expect: `STATUS.md`, `claude/steering-prompts/08-dependency-pinning-task-prompt.md`, `claude/steering-prompts/09-tool-quirks-indexing-research-prompt.md`, `claude/tool-quirks.md` — `4 files changed, 106 insertions(+), 2 deletions(-)`.

2. **Claim: `claude/tool-quirks.md` (at that commit) gets a new entry about gh api pulls/<N> reporting a stale head.sha after a fast push.**
   `git show b8d07f9:claude/tool-quirks.md | grep -n "stale .head.sha\|head.sha.*after a fast push"`
   Expect: at least one match, status tagged `[Diagnosed`.

3. **Claim: the new `claude/steering-prompts/08` task prompt (at that commit) scopes dependency pinning for ast-grep-cli/sqllineage/pathspec, referencing CONSTRAINTS.md and MATURITY_ASSESSMENT.md.**
   `git show b8d07f9:claude/steering-prompts/08-dependency-pinning-task-prompt.md | head -5`
   Expect: frontmatter naming "Dependency pinning" and referencing both files.

4. **Claim: the cherry-picked commit f3af862 was not yet an ancestor of `13553ba` (PR #13's own merge commit) — it was genuinely stranded, not an already-merged duplicate.**
   `git merge-base --is-ancestor f3af862bfd765f26b5e837499cb60010ab966f0b 13553ba; echo "exit=$?"`
   Expect: `exit=1` (not an ancestor — confirms this commit was missing from `main` at that point).
