---
pr: 10
title: "Log PR #9 review findings; scaffold task prompt for repo's first CI job"
state: MERGED
branch: ci-scaffold-task-prompt -> main
merge_commit: 19714dddbec4171ee66e3cc4a00f991f61fb659f
---

# PR #10 — Log PR #9 review findings; scaffold task prompt for repo's first CI job

## Summary

Doc-only PR. Adds two new `CONSTRAINTS.md` findings from a 2026-07-23 review of PR #9's GitHub page: "Integration gaps" item 4 (`claude/llms/pr-N.md`'s hand-verified commands have no automated re-check — a meta-drift risk as `main` evolves) and "Enterprise-readiness gaps" item 6 (PRs land with zero required checks/reviews, no confirmed branch protection on `main` — observed directly as `Checks: 0`, `No reviews`, `1 participant`, still "Ready to merge"). Updates `STATUS.md`'s Pending/"Next concrete action" sections to match. Adds `claude/steering-prompts/07-ci-scaffold-task-prompt.md`, a self-contained implementation task prompt (following the `06-...` template) scoping a future session's work to build `scripts/verify_llms_docs.py` and this repo's first `.github/workflows/` CI job — explicitly scoped to **not** touch branch-protection settings, flagged instead as a repo-admin follow-up.

## Deterministic verification

Pinned to `19714dd`:

1. **Claim: CONSTRAINTS.md gets a new "meta-drift" integration gap (item 4) naming claude/llms/'s lack of automated re-verification.**
   `git show 19714dd:CONSTRAINTS.md | grep -n "meta-drift"`
   Expect: one match, item 4 under "Integration gaps," referencing `claude/steering-prompts/07-ci-scaffold-task-prompt.md`.

2. **Claim: CONSTRAINTS.md gets a new enterprise-readiness gap (item 6) documenting PR #9's zero-checks/zero-reviews state.**
   `git show 19714dd:CONSTRAINTS.md | grep -n "Checks: 0\|No reviews"`
   Expect: one match, quoting `Checks: 0`, `Reviewers: No reviews`.

3. **Claim: adds a new self-contained task prompt scoping the CI-scaffold work, explicitly excluding branch-protection changes.**
   `git show 19714dd:claude/steering-prompts/07-ci-scaffold-task-prompt.md | head -5`
   Expect: frontmatter naming the CI-job/meta-verification task and referencing both `CONSTRAINTS.md` items above.

4. **Claim: documentation-only change — 3 files touched, no code.**
   `git show --stat --format="" 19714dd`
   Expect: `CONSTRAINTS.md`, `STATUS.md`, and the new `claude/steering-prompts/07-ci-scaffold-task-prompt.md` only — `3 files changed, 54 insertions(+), 7 deletions(-)`.
