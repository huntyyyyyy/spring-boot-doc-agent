{
  "$comment": [
    "Committed, shared project settings. Until now .claude/ held only a",
    "settings.local.json that this machine's global gitignore excludes, so",
    "nothing here was versioned or shared.",
    "",
    "Scope is deliberately narrow: this repo's own deterministic, read-only",
    "checkers and test suites, so a session can run the gates without a",
    "prompt for each one. It does not carry the broad wildcards the local",
    "file accumulated (git push, gh api, bare python) -- those are a",
    "convenience for one machine, not a property of the project, and an",
    "allowlist that permits everything documents nothing.",
    "",
    "Two entries in the local file name scripts that do not exist:",
    "verify_llms_docs.py, deleted as an RCE vector, and",
    "test_check_session_log.py, which lives only on the parked branch.",
    "Neither is carried over.",
    "",
    "The ast-grep entries below are load-bearing, not convenience. The five",
    "pipeline agents used to declare 'tools: Read, Grep, Glob, Write'. A",
    "subagent's tools: field accepts bare tool names only -- it cannot",
    "express Bash(ast-grep:*) -- so file-summarizer and doc-writer now",
    "declare bare Bash, and the ONLY thing keeping that grant narrow is the",
    "allowlist here. Widening it widens fourteen parallel doc-writer",
    "dispatches at once. The matching deny entries stop the same agents",
    "reaching for text search instead; scripts/ci/check_repo_claims.py fails",
    "the build if an agent regains Grep or declares Bash without a scoped",
    "entry here.",
    "",
    "The semgrep entries are the same pattern for a sixth agent,",
    "software-architect-and-testing (agents/software-architect-and-",
    "testing.md), which also declares bare Bash. It runs",
    "scripts/coverage/spring_semgrep_rules.yml (the curated DDIA/testing-smell",
    "ruleset) plus, occasionally, a community registry config -- 'semgrep",
    "scan:*' covers both without granting an unscoped Bash.",
    "",
    "The curl/wget/git-clone denies below are the network-egress analog of",
    "the grep/rg denies above: software-architect-and-testing is the only",
    "agent granted both Bash and WebFetch today, and nothing stopped a raw",
    "shell command from reaching arXiv/GitHub/deepwiki.com directly instead",
    "of going through WebFetch, bypassing the tiering/citation discipline",
    "its own prompt establishes. git clone is denied by subcommand, not bare",
    "git, since git status/diff/log/ls-files above are already legitimately",
    "allowed. check_repo_claims.py's check F fails the build if an agent",
    "declares Bash without these three denies present, the same as it",
    "already does for the grep/rg denies.",
    "",
    "doc-engine query / query_artifacts entries are read-only Stage-0 artifact",
    "lookups (capped typed views). Prefer them over Read-ing whole",
    "spring_signals.json when a run dir exists — see adapters/claude/SEARCH.md."
  ],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"adapters/claude/hooks/require_hardened_tests.py\""
          }
        ]
      },
      {
        "$comment": [
          "Added 2026-07-25 after this exact mistake produced two false 'green'",
          "reports in one session: `gradle ... | tail -30` then `echo \"EXIT=$?\"`",
          "reads tail's exit code, not gradle's -- tail always exits 0. Runs on",
          "every Bash call (cheap regex over the command string); denies with a",
          "corrective pattern rather than asking, since a human is not always",
          "present to unblock a background session. See .claude/hooks/",
          "check_pipe_exit_code.py for the full reasoning and its escape hatches",
          "(PIPESTATUS, pipefail).",
          "",
          "A second hook (enforce_repo_claims_before_commit.py) was written",
          "alongside this one to run check_repo_claims.py before every git",
          "commit, then deleted on discovering hooks/require_hardened_tests.py",
          "already does that plus check_code_quality.py plus missing-test-sibling",
          "and unwired-CI-suite checks. Do not re-add it -- extend",
          "require_hardened_tests.py instead if this gate needs to grow."
        ],
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 .claude/hooks/check_pipe_exit_code.py",
            "timeout": 10
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(python3 scripts/ci/check_repo_claims.py)",
      "Bash(python3 scripts/ci/check_repo_claims.py --fix)",
      "Bash(python3 scripts/ci/check_code_quality.py)",
      "Bash(python3 scripts/ci/check_llms_coverage.py)",
      "Bash(python3 scripts/coverage/rule_coverage.py:*)",
      "Bash(python3 -m unittest discover -s scripts -p \"test_*.py\")",
      "Bash(python3 -m ruff check --no-cache scripts/)",
      "Bash(ast-grep run:*)",
      "Bash(ast-grep scan:*)",
      "Bash(ast-grep test:*)",
      "Bash(ast-grep --version)",
      "Bash(semgrep scan:*)",
      "Bash(semgrep --version)",
      "Bash(doc-engine query:*)",
      "Bash(python -m doc_engine.tools.query_artifacts:*)",
      "Bash(python -m doc_engine.cli query:*)",
      "Bash(python adapters/mcp/server.py:*)",
      "Bash(python -m adapters.mcp.server:*)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git ls-files:*)"
    ],
    "deny": [
      "Grep",
      "Bash(grep:*)",
      "Bash(egrep:*)",
      "Bash(fgrep:*)",
      "Bash(rg:*)",
      "Bash(ripgrep:*)",
      "Bash(curl:*)",
      "Bash(wget:*)",
      "Bash(git clone:*)"
    ]
  }
}
