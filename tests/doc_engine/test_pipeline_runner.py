"""Pipeline runner suites (split by concern).

See sibling runner modules under `tests/doc_engine/` and
`tests/support/pipeline_runner/`.
"""

import pytest

pytestmark = pytest.mark.domain_pipeline

